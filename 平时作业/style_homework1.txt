学号：2019211424
姓名：胡敏臻
程序：fifo_queue.c
问题1：7行存在类里面的成员不需要再重复类名问题，应修改为 int value;
问题2：18、33行存在函数名称过长问题，命名可以更加简单，可以直接就表示为添加和修改，应修改为push_back_queue/pop_front_queue
问题3：32行存在没有写清楚返回值的意义的文体，应修改为，需要再添加一行注释 //如果删除成功，返回1，如果删除失败返回0
问题4：4行存在注释没有意义的问题，此时的注释写了和没写一样，并且中英文注释不同意 应修改为 //每个队列的单元定义为值和指向下一个单元的指针

程序：serialize_json.cpp
问题1：22行存在使用表达式不自然问题，前面还加了否定符号，应修改为if ( (*c <' \x00 ') || (*c >' \x1f ') )
问题2：32行存在函数命名问题，没有在函数名中说明序列化为什么，应修改为 string serialize_json(map<string,string>&m)
问题3：37行存在否定命名问题，会出现两个否定，意思表达不够清晰，应修改为 bool is_first=true;
问题4：43行存在函数长度过长，不利于阅读的问题，应该修改为在适当处换行写。

程序：split_str.cpp
问题1：25行存在神秘的数字问题，我们不知道31的具体含义是什么，应该在程序开始出先定义 例如 可以修改为 #define maxsize 31
问题2：20行存在加上括号可读性更强，当三个条件判断时可以增加括号来提高可读性，应修改为 while( (buffer[i] != ' ' ) &&  (buffer[i] != '\t')  && (buffer[i] != 0) )
问题3：7行存在返回值说明不准确问题，仅仅只说了正确返回>0，与第一行相距较远，不利于阅读，应修改为将第4行和第7行合并在一处
问题4：第25行判断可以放在while判断的条件中，这样使程序更加简便，在循环中少了跳转，应修改为while( (buffer[i] != ' ' ) &&  (buffer[i] != '\t')  && (buffer[i] != 0)&&(j<31) )
